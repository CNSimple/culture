# 燕赵遗韵项目进展与状态报告

**报告日期：** 2026年9月16日  
**统计范围：** 仓库当前可见提交记录（原型阶段）至本次知识图谱交互优化完成  
**当前分支：** `zxq`  
**当前版本基线：** `e94dff7`（与远程分支 `origin/zxq` 一致）

## 一、项目概况

“燕赵遗韵”目前已形成一个以 Flask 为入口、Vue 3 为知识图谱前端的交互式文化展示原型。整体页面涵盖文化地图、成语档案、知识图谱、AI 问答、图片识别、研学助手和沉浸剧情等入口，其中知识图谱已从早期的少量示例节点扩展为可筛选、可聚焦、可拖拽和可缩放的独立页面。

当前知识图谱使用 Vue 3、TypeScript、Vite、Vue Router、Pinia 和 D3 构建，生产资源由 Flask 项目的 `static/graph-app` 目录托管。

## 二、阶段更新记录

### 1. 原型基础阶段

对应提交：`a704f6e`（原型）

该阶段完成项目的基础交互原型，为后续独立知识图谱页面、数据接入和前端组件化提供了基础。

### 2. 知识图谱 Vue 化并接入 Flask

对应提交：`e180e8b`  
提交时间：2026年9月14日 22:31

主要更新如下：

- 新建 Vue 3 + TypeScript + Vite 前端工程。
- 将知识图谱拆分为独立页面，并通过 Flask 路由和静态资源目录接入原型。
- 建立顶部导航、实体筛选侧栏、图谱画布、实体详情、历史时间轴等组件。
- 使用 D3 实现节点和关系绘制，并加入节点聚焦、关系动画与基础交互。
- 引入 Pinia 管理知识图谱选中状态、筛选状态和详情面板状态。
- 加入图谱背景、人物图像、典故场景图以及移动端、桌面端预览资源。
- 更新项目说明和启动方式。

本次提交规模为 53 个文件，新增约 4253 行代码。

### 3. 完整数据接入与图谱功能增强

对应提交：`e94dff7`  
提交时间：2026年9月16日 18:55

主要更新如下：

- 接入《邯郸成语典故数据集》。
- 将图谱扩充至 **408 个文化实体、459 条实体关系**。
- 节点类型扩展为成语、历史人物、古迹遗址、史料文献和历史事件五类。
- 新增成语与人物图像素材切片映射。
- 重构图谱局部展开逻辑，支持从中心节点逐级展开相邻关系。
- 增加“局部展开”“展示全部节点”“只展示所有成语”三种视图模式。
- 完善节点拖拽、力导向布局、碰撞检测和关系曲线显示。
- 完善实体详情面板，包括关联信息、节点说明、数据概览和研学拓展。
- 完善分类筛选、朝代多选和图例说明。
- 更新生产构建文件并同步到 Flask 静态资源目录。

本次提交涉及 22 个文件，新增约 805 行、删除约 530 行。

### 4. 本次交互体验优化

当前状态：**已完成开发和构建验证，尚未提交 Git**。

本次根据使用反馈完成两项调整：

1. **移除点击节点后的黑色矩形框**
   - 原因是 SVG 节点具有键盘焦点能力，浏览器在鼠标点击后绘制了默认矩形焦点轮廓。
   - 当前已取消该默认矩形轮廓。
   - 仍保留节点圆环高亮，确保键盘操作时能识别焦点位置。

2. **新增右下角放大、缩小按钮**
   - 在知识图谱画布右下角增加“＋”和“−”控件。
   - 每次点击按平滑动画执行 1.25 倍放大或 0.8 倍缩小。
   - 缩放范围继续沿用图谱原有限制：0.45 倍至 4 倍。
   - 原有鼠标滚轮缩放和平移功能保持不变。

涉及源文件：

- `frontend/src/components/GraphViewport.vue`
- `frontend/src/styles/theme.css`

构建后同步更新：

- `static/graph-app/index.html`
- `static/graph-app/assets/index-DL50G9IF.css`
- `static/graph-app/assets/index-Dw9NZQjY.js`

旧的带哈希构建文件已由 Vite 自动替换。

## 三、当前功能状态

| 模块 | 当前状态 | 说明 |
|---|---|---|
| Flask 原型入口 | 可用 | 可托管主原型和知识图谱生产资源 |
| 知识图谱页面 | 可用 | 已独立为 Vue 页面 |
| 图谱数据 | 已接入 | 408 个节点、459 条关系、5 种类型 |
| 节点点击与详情 | 可用 | 支持选中、聚焦及关联详情展示 |
| 节点展开 | 可用 | 支持局部逐级展开 |
| 全量/成语视图 | 可用 | 支持全节点及成语节点切换 |
| 分类与朝代筛选 | 可用 | 支持多条件筛选 |
| 节点拖拽 | 可用 | 拖拽后可保持节点位置 |
| 鼠标缩放和平移 | 可用 | 缩放范围为 0.45～4 倍 |
| 右下角缩放按钮 | 已新增 | 支持平滑放大和缩小 |
| 节点黑框问题 | 已修复 | 默认矩形焦点框已移除 |
| 全屏和重新布局 | 可用 | 保留原有工具按钮 |
| 生产构建 | 已通过 | TypeScript 检查与 Vite 构建成功 |

## 四、验证结果

已执行以下验证命令：

```powershell
cd frontend
npm run build
```

验证结果：

- `vue-tsc --noEmit` 通过，未发现 TypeScript 类型错误。
- Vite 生产构建通过。
- 共转换 614 个模块。
- 生产资源已输出至 `static/graph-app`。
- 当前生成的主要资源为：
  - CSS：约 40.86 kB，gzip 后约 9.81 kB。
  - JavaScript：约 246.29 kB，gzip 后约 85.26 kB。

## 五、版本与工作区状态

当前分支关系：

- `main`：位于 `e180e8b`，与 `origin/main` 一致。
- `zxq`：位于 `e94dff7`，与 `origin/zxq` 一致。

当前工作区存在尚未提交的本次优化：

- 修改：`frontend/src/components/GraphViewport.vue`
- 修改：`frontend/src/styles/theme.css`
- 修改：`static/graph-app/index.html`
- 删除：上一版本带哈希的 CSS、JavaScript 构建文件。
- 新增：本次构建生成的带新哈希 CSS、JavaScript 文件。
- 新增：本进展报告。

因此，当前功能代码和构建产物已经完成，但在提交、推送前仍属于本地工作区变更。

## 六、已知事项与建议

1. `frontend/README.md` 中仍描述图谱只有五个实体，与目前 408 个节点的实际状态不一致，建议后续更新文档。
2. 建议在浏览器中补充一次桌面端和移动端人工验收，重点检查右下角缩放控件是否遮挡图谱内容。
3. 建议将本次节点焦点框修复、缩放按钮和构建产物作为一个独立提交，便于版本追踪。
4. 正式发布前，建议由相关人员进一步核对典故出处、年代及地点对应关系。
5. 当前 AI 问答、图片识别和沉浸剧情仍以原型演示能力为主，尚未接入正式大模型、图像识别模型或 3D 引擎。

## 七、总体结论

项目已由基础 Flask 交互原型推进为具备独立 Vue 知识图谱模块的可运行版本。知识图谱已完成全量数据接入、节点关系展示、分类与朝代筛选、详情查看、拖拽、平移、滚轮缩放、按钮缩放及全屏浏览等主要交互。本次反馈中的节点黑框问题和右下角缩放操作均已解决，TypeScript 检查与生产构建通过。

当前总体状态为：**功能可运行，最新优化已完成并生成生产资源，等待人工界面验收及 Git 提交、推送。**
